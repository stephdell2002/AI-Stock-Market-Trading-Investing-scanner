# Environment / secrets — API keys live in env vars only, never in the repo
.env

# Runtime artifacts — the repo-root data/ dir ONLY (anchored with a leading
# slash so it never matches the source package src/watchman/data/).
/data/
*.db
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
